import { AdminPraticeSession } from './admin-pratice-session.model';

describe('AdminPraticeSession', () => {
  it('should create an instance', () => {
    expect(new AdminPraticeSession()).toBeTruthy();
  });
});
